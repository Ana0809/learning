package digitalLibrary;

import java.util.ArrayList;
import java.util.List;

public class Library{
    ArrayList<MediaEntity> top20 = new ArrayList<>();              // keep only the last 20 most accessed media entities
    ArrayList<MediaEntity> archive = new ArrayList<>();            //keep only the last 20 most accessed media entities

}
